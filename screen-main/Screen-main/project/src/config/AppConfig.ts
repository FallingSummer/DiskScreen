import type { AppConfig as AppConfigType } from '@/types/GlobalTypes';

export const AppConfig: AppConfigType = {
  appName: 'DataScreenPractice',
  version: '0.0.1',
  environment: 'development',
};
